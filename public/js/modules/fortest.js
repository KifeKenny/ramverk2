/**
 * Just a module so that i can check that testing works.
 *
 * @module
 */
"use strict";

class ForTest {
    mulitplikation(num1, num2) {
        var result = num1 + num2;

        return result;
    }
}


// Export module
module.exports = ForTest;
